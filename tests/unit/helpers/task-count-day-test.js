
import { taskCountDay } from 'wellness-challenge/helpers/task-count-day';
import { module, test } from 'qunit';

module('Unit | Helper | task count day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taskCountDay([42]);
  assert.ok(result);
});

