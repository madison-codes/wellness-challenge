
import { taskCountTotal } from 'wellness-challenge/helpers/task-count-total';
import { module, test } from 'qunit';

module('Unit | Helper | task count total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taskCountTotal([42]);
  assert.ok(result);
});

