<p>home</p>
{{#if (eq session.data.authenticated.profile.name 'hello')}}
  {{ admin-dashboard session=session }}
{{else}}
  {{ student-create student=model createAction="saveUser"}}
  {{ task-form tasks=model createAction="submitTasks" }}
{{/if}}
